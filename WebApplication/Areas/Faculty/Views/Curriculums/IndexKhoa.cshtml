@model IEnumerable<WebApplication.Models.Khoa>

@{
    ViewBag.Title = "IndexKhoa";
    Layout = "~/Views/Shared/Faculty_Layout.cshtml";
}

<h2>IndexKhoa</h2>

<p>
    @Html.ActionLink("Tạo mới", "CreateKhoa")
    @*<form>
        <button type="submit" id="button" formaction="@Url.Action("CreateKhoa")" class="btn btn-secondary btn-rounded">
            Tạo mới
        </button>
    </form>*@    
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenKhoa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenKhoa)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditKhoa", new { id=item.ID }) |
            @Html.ActionLink("Delete", "DeleteKhoa", new { id=item.ID })
        </td>
    </tr>
}

</table>
